# Vireo Starter documentation examples

This private Gradle module compiles the exact Java source displayed by the unified Vireo Starter Storybook.

It is not published and applications must not depend on it. Its only job is to make stale or uncompilable documentation fail the ordinary JVM build.
