/**
 * Stable HTTP error, pagination, principal, and OpenAPI helper contracts.
 *
 * <p>
 * Unexpected exceptions are logged without exposing internal details by
 * default. Request utilities reject invalid public inputs explicitly rather
 * than allowing framework exceptions to become accidental HTTP 500 responses.
 */
package com.vireocode.web;
