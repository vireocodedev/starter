description = 'A replaceable default user model, UserDetailsService and session endpoints. Every bean here is meant to be overridable.'

dependencies {
    api project(':vireo-starter-core')
    annotationProcessor 'org.springframework.boot:spring-boot-configuration-processor'
}
