---
"@vireocodedev/starter-sqlite": major
---

Rebuild SQLite as a framework-free, worker-safe persistence package with managed Worker lifecycle, validated typed entity bundles, explicit migrations, OPFS ownership, persistent offline queue and replay, hydration coordination, operation serialization, SQL-console ports, transport-neutral offline utilities, and executable documentation.

Remove application entity, HTTP, cache, signal, and UI ownership while preserving dedicated root and `/offline` entry points.
