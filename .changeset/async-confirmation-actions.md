---
"@vireocodedev/starter-ui": minor
---

Allow `VireoConfirmationProvider` requests to execute asynchronous confirmation actions while preserving and locking the dialog until success, with retry after rejection.
