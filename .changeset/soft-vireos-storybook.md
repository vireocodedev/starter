---
"@vireocodedev/starter-ui": minor
---

Add explicit Storybook developer-tooling entry points, including the shared `VireoStorybookProvider` and the component-specific `storybook/VireoIconContainer` comparison frame. Display VireoIconContainer stories from executable, copy-pastable source modules instead of generated private render wrappers.
