---
"@vireocodedev/starter-ui": minor
---

Route the default `VireoQueryBoundary` loading fallback through the shared delayed loading-region contract. Add `announceLoading` for nested-boundary ownership and `loadingRevealDelay` for explicit timing overrides; custom fallbacks continue to own their own semantics.
