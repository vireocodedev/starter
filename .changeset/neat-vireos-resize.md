---
"@vireocodedev/starter-ui": minor
---

Add `VireoSidePanelResizeHandle`, a themeable and slot-based pointer interaction target for resizing Vireo side panels. Keep `SidePanelResizeHandle` as a deprecated compatibility alias.
