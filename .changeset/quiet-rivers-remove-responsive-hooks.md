---
"@vireocodedev/starter-ui": major
---

Remove the legacy device, responsive-prop, container-size, and window-resize hooks together with the unused mobile document-attribute provider. Global snackbar placement now uses an explicit local viewport query, while Vireo responsive presentation follows the documented container-aware policy.
