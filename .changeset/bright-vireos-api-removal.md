---
"@vireocodedev/starter-ui": major
---

Remove the legacy `./api` entry point and its UI-owned Axios, pagination, error-serialization, and Zod parsing helpers. Network and response parsing contracts now belong to `@vireocodedev/starter-infrastructure`.
