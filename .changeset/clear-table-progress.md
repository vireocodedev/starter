---
"@vireocodedev/ui": minor
---

Add localized incremental-loading labels to `VireoResponsiveTable`, give the mobile progress indicator an accessible name, and announce both loading and completion through one stable polite status region.
