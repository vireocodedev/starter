---
"@vireocodedev/starter-queryengine": major
---

Rebuild Query Engine as a framework-free, worker-safe package for Zod-validated entity metadata, injected transport ports, fail-closed filter compilation, parameterized SQLite execution, instance-scoped configuration persistence, deterministic concurrency, and executable documentation.

Move React Query caching and hooks to Starter UI and remove application-owned entity, transport, signal, and cache policy from the package.
