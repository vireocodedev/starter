---
"create-vireo": patch
---

Keep projected frontend Doctor automation consistent with the full-stack report contract by adding a pure JSON mode and its `doctor:json` package script. The 0.8.1-to-0.8.2 managed upgrade preserves the creation identity while recording the immutable Template tag/version/commit and applying the exact Doctor baselines with customization refusal and idempotence.
