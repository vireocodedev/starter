---
"@vireocodedev/starter-ui": minor
---

Add `VireoIcon`, `VireoIconRegistryProvider`, and `useVireoIcons` as the canonical typed icon-registry APIs. Preserve the former `RgoIcon`, provider, registry, and hook names as deprecated aliases.
