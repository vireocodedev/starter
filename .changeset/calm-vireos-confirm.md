---
"@vireocodedev/starter-ui": major
---

Replace `RgoConfirmProvider` and `useRgoConfirm` with `VireoConfirmationDialog`, `VireoConfirmationProvider`, and the promise-based `useVireoConfirmation` API. Confirmation message strings are now rendered safely as text instead of parsed as HTML.
