---
"@vireocodedev/starter-localization": minor
---

Add `network.connectingToLiveUpdates` and `network.hydratingInBackground` to the platform namespace so consumers can localize offline hydration and live-update banners.
