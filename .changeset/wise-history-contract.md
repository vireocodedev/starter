---
"@vireocodedev/starter-history": major
"@vireocodedev/starter-ui": patch
"@vireocodedev/starter-infrastructure": patch
"@vireocodedev/starter-queryengine": patch
---

Complete the History package audit with safe canonical comparison, lossless typed paths, empty-container changes, definition-derived snapshot inference, sequence-aware ordered moves, early runtime configuration validation, typed record-schema options, and comprehensive executable documentation.

Fix `VireoHistoryEntry` path keys and `emptyValue` rendering, and constrain packages with Zod 3-shaped public contracts to the supported Zod 3 peer range.
