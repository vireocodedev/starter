---
"@vireocodedev/ui": patch
---

Compose `VireoInfiniteCanvas` root pointer handlers before built-in panning and honor `defaultPrevented`, matching the event-cancellation contract used by other slotted Vireo components.
