---
"@vireocodedev/starter-ui": major
---

Add `VireoResponsiveFormOverlay` as the canonical responsive, unsaved-change-aware form overlay and remove the legacy `ResponsiveFormOverlay` and `useDeferredSuccessNotification` APIs.
