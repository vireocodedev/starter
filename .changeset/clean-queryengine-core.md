---
"@vireocodedev/starter-queryengine": major
"@vireocodedev/starter-ui": minor
---

Complete the Query Engine package audit with a framework-free, worker-safe core; strict Zod metadata and persisted-config validation; fail-closed filter compilation; normalized SQLite pagination; portable source imports; and comprehensive executable documentation in the unified Vireo Storybook.

Remove the package-root React Query factory and global React signals. Add the side-effect-free `createVireoQueryEngineQueries` adapter to `@vireocodedev/starter-ui/tanstack-query` for React consumers.
