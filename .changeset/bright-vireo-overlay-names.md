---
"@vireocodedev/ui": major
---

Require every `VireoResponsiveOverlayFrame` to provide either `aria-label` or `aria-labelledby`, and expose the selected overlay side panel as a named dialog surface.
