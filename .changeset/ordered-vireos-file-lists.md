---
"@vireocodedev/starter-ui": minor
---

Add `field.FileListField` to `useVireoForm` with ordered `File[]` values, append and partial-acceptance semantics, duplicate and capacity constraints, accessible reordering, responsive filename truncation, opt-in previews, validation integration, tests, and complete Storybook examples.
