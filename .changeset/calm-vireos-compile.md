---
"@vireocodedev/starter-ui": patch
---

Replace compiler-expensive global MUI component-map augmentation with direct, strongly typed Vireo theme contracts so declaration builds and typechecks run under Node's default heap.
