---
"@vireocodedev/ui": minor
---

Require a localized landmark label on `VireoApplicationNavigation` and a destination URL on each `VireoApplicationNavigationItem`. Navigation content now renders as a named `nav` landmark, while items render as links with current-page and disabled semantics.
