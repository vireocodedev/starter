---
"@vireocodedev/starter-ui": major
---

Remove the whole-form `form.ResetButton` contract and make `VireoFormActions` keep actions in one horizontal row. Ordinary action buttons now share the available width equally, while optional overflow icon buttons retain their intrinsic width.
