---
"@vireocodedev/starter-ui": major
---

Replace the legacy provider array, theme wrapper, and destructive theme-color provider with `VireoProviderComposer` and the reversible `VireoThemeColorMeta` bootstrap contracts.
