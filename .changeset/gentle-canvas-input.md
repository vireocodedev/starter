---
"@vireocodedev/ui": minor
---

Preserve normal page wheel and touch scrolling around `VireoInfiniteCanvas` by default. Wheel zoom and touch panning are now explicit opt-ins through `wheelZoomEnabled` and `touchPanEnabled`.
