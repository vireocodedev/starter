---
"@vireocodedev/starter-ui": patch
---

Render every loaded mobile responsive-table row directly, restore independently stateful animated accordions without whole-list rerenders, and remove the TanStack Virtual peer dependency while preserving incremental loading and scroll restoration.
