---
"@vireocodedev/starter-shell": major
---

Replace the React application-shell framework with framework-free, worker-safe contracts for typed sitemap construction, navigation metadata, safe authentication redirects, and instance-scoped overlay-history coordination, backed by Zod validation and executable documentation.

Remove the legacy React Router adapters, responsive layouts, MUI navigation components, PWA UI, React hooks, app-specific console policy, and global overlay store.
