---
"@vireocodedev/starter-ui": major
---

Replace the legacy tab and generic URL-state hooks with the typed `useVireoSearchParamState` core contract and explicit built-in codecs, and remove the now-unused `react-use` dependency.
