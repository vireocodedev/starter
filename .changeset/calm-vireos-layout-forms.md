---
"@vireocodedev/starter-ui": minor
---

Add container-responsive form layout primitives through `form.Section`, `form.SectionItem`, and `form.Actions`, and add standard width presets to `form.Form`. Export the underlying `VireoFormSection`, `VireoFormSectionItem`, and `VireoFormActions` components for native-form compositions.
