---
"@vireocodedev/starter-ui": major
---

Remove the `configureAxiosClient` package API and the UI package's Axios dependency. Applications now own network-client defaults and interceptor policy in their infrastructure layer.
