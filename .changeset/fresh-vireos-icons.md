---
"@vireocodedev/starter-ui": minor
---

Add `VireoIconContainer`, a slot-based and themeable replacement for `RgoIconContainer` that scales SVG geometry into the standard 24×24 icon coordinate system. Keep the former component and props names as deprecated package-root aliases for downstream compatibility.
