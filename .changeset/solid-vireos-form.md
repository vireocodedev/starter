---
"@vireocodedev/starter-ui": minor
---

Establish the forms capability and add the full accessible `VireoFormSection` contract. Add the TanStack-bound `useVireoForm` API with `form.Form`, `field.TextField`, `field.NumberField`, `field.SwitchField`, `form.SubmitButton`, and `form.ResetButton`. Preserve `RgoFormSection` as a deprecated adapter.
