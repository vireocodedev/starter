---
"@vireocodedev/starter-ui": minor
---

Add `field.FileField` with single-file picker, drag-and-drop, rejection, responsive metadata, and opt-in preview behavior. Add the public `VireoFileImagePreview` renderer through the forms entry point.
