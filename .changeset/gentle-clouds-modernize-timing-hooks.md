---
"@vireocodedev/starter-ui": major
---

Replace `useRgoDebounce` with `useVireoDebouncedCallback`, providing typed trailing scheduling, cancellation, flushing, pending inspection, latest-callback behavior, and lifecycle cleanup. Remove the unowned `useSingleFlightAction` and `useRgoAutoDismiss` APIs.
