---
"@vireocodedev/starter-ui": minor
---

Add `VireoDelayedRender`, a slot-based and themeable utility for suppressing short-lived fallback flashes while preserving layout. Keep `DelayedRender` as a deprecated compatibility alias.
