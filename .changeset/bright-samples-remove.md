---
"create-vireo": patch
---

Track the full-stack Overview sample in example provenance so upgrades and sample removal fail closed on customized bytes, and stage the immutable 0.8.5 projection compatibility upgrade.
