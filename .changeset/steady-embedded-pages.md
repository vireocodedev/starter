---
"@vireocodedev/ui": patch
---

Initialize uncontrolled `VireoPage` layouts from one deterministic server and hydration mode before resolving the actual embedded container, and apply reserved inline space to every container measurement.
