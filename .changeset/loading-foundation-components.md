---
"@vireocodedev/starter-ui": minor
---

Add `VireoSkeleton` for silent geometry-preserving placeholders and `VireoLoadingRegion` for shared reveal timing, busy semantics, and a single polite loading announcement.
