# Repository migration verification — 2026-09-01

Canonical repositories are `vireocodedev/vireo` and
`vireocodedev/vireo-template`. Anonymous old repository, tag, and clone URLs were
verified to redirect to their canonical counterparts. Canonical GitHub Pages routes
under `vireocodedev.github.io/vireo/` also passed.

Historical `vireocodedev.github.io/starter/*` project-pages paths return 404 rather
than redirect. Current public source links were migrated; immutable provenance and
dated historical evidence retain their original URLs. This Pages limitation is
recorded as history compatibility, not a claim that every former Pages URL redirects.
