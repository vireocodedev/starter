# Phase 1 progress — 2026-08-27

Status: **in progress; P1-06–P1-08 and P1-10–P1-14 complete; P1-09 source-owned tranche complete**

## G-110 public documentation completion

G-110 is closed. Starter now publishes one release-specific documentation
snapshot at
[`npm-0.2.1_jvm-0.2.0`](https://vireocodedev.github.io/starter/versions/npm-0.2.1_jvm-0.2.0/)
while preserving the existing root Storybook. Stable
[`/docs/`](https://vireocodedev.github.io/starter/docs/),
[`/latest/`](https://vireocodedev.github.io/starter/latest/), TypeScript API, JVM
API, human version-index, and machine version-index routes resolve to the declared
current npm/JVM pair. Both public repository homepages and both READMEs now use the
canonical `/docs/` entry point.

The source-owned release contract matches all seven npm manifests at `0.2.1` and
the six-module JVM family at `0.2.0`. It requires prior snapshots to become
complete retained archives before a future current release can replace the stable
aliases. The generated artifact contains and searches:

- 150 Storybook guide/component-documentation pages and 317 component examples;
- 1,837 exports across all 22 declared TypeScript package entry points;
- 107 public JVM types and 559 members from aggregate Javadocs; and
- 2,970 records in total, with checked targets and checked representative search
  queries.

The Pages workflow now builds package declarations, Storybook, and aggregate
Javadocs; assembles the versioned portal and release links; rejects manifest,
route, count, anchor, search-example, or target drift; and only then uploads the
static artifact. The complete local Starter gate passed all 11 TypeScript/docs
stages in 83.983 seconds, followed by the JVM build/Javadocs, portal artifact audit,
six-module publication audit, and isolated versionless Maven consumer.

Hosted Pages run
[`33089179119`](https://github.com/vireocodedev/starter/actions/runs/33089179119)
built, audited, uploaded, and deployed the corrected artifact successfully from
commit `f2e7d2e`. Anonymous requests returned 200 for the stable portal, latest,
versions, machine index, release snapshot, search index, TypeScript API, JVM API,
and versioned Storybook routes. The live index reported the same 2,970 records and
returned results for `VireoPageLayout`, `offline replay`, and `BaseService`.

## Completed local implementation

- Added support, governance, ownership interpretation, response boundaries, and
  structured bug/feature/security intake to Starter and Template.
- Added a Starter compatibility policy covering independent npm and JVM release
  families, SemVer, peer floors, public surfaces, configuration and schema behavior,
  cross-stack deployment, deprecation, supported release lines, and migration.
- Expanded the Template compatibility manifest with its exact six npm ranges, JVM
  version, lockfile boundary, application-owned upgrade flow, and deployment-order
  responsibility.
- Added both policies to the authoritative repository gates. The checks derive
  current versions from manifests, require community-health surfaces, validate form
  structure, and traverse local Markdown links.
- Corrected 11 stale Starter Markdown links exposed by the new check.

## Publication and P1-06 live settings

Both verified branches were pushed to GitHub. Live metadata was then changed as
follows:

- `vireocodedev/starter`: accurate product description, public Storybook homepage,
  nine reviewed topics, and Discussions enabled.
- `vireocodedev/starter-template`: accurate Template description, public docs
  homepage, nine reviewed topics, Discussions enabled, and Template-repository mode
  enabled.

Authenticated API reads confirm every setting. Anonymous HTTP checks return 200 for
the Starter repository, support, governance, compatibility, issue chooser, bug form,
and Discussions.

The same audit initially found `vireocodedev/starter-template` private, contrary to
the earlier roadmap record. The owner then made it public. A fresh authenticated API
read confirms public visibility, and anonymous HTTP checks now return 200 for its
repository, support, governance, compatibility, issue chooser, bug form, and
Discussions. P1-06, P1-07, and P1-08 are therefore complete.

Repository renames, professional clearance, and account recovery remain separate
work and were not changed in this slice.

## P1-09 security hardening

An authenticated provider audit found and corrected safe, reversible settings on
both repositories: private vulnerability reporting, Dependabot alerts and security
updates, secret scanning, push protection, non-provider patterns, validity checks,
read-only default workflow tokens, disabled workflow PR approvals, and required
full-SHA action pinning are enabled.

Starter now adds CodeQL for Java and JavaScript/TypeScript plus moderate-severity
dependency review. Template now adds the same pinned full-history Gitleaks control
as Starter and removes an unnecessary pull-request write permission. Starter release
evidence now generates a populated JVM CycloneDX SBOM alongside its npm SBOM. The
[dated recovery tabletop](../../security/recovery-exercise-2026-08-27.md) records
the tested response sequence and remaining provider/human limitations.

Starter now also owns a credential-free post-publication collector and attestation
workflow. It independently downloads all seven npm `0.2.1` tarballs and 27 Maven
Central `0.2.0` files, validates registry SRI/provenance and Central SHA-256, then
binds the exact subjects to populated npm/JVM CycloneDX SBOMs through GitHub
OIDC/Sigstore. Verification enforces the CycloneDX predicate, exact workflow
certificate identity, and `main` source ref before retaining the evidence for 90
days.

P1-09 remains partial. The authenticated 2026-08-31 record confirms the live
no-bypass main/tag rulesets, narrowed provider Actions allowlist, valid
CODEOWNERS, re-audited security settings, and Maven/Pages/package environment
restrictions. The four environment administrator-bypass toggles remain a UI-only
provider action. Only one trusted owner is evidenced, so independent review and
backup-owner recovery have not been exercised.

P1-09 verification evidence:

- authenticated API reads returned enabled for every provider control listed above
  on both repositories;
- Template Gitleaks scanned 86 commits and about 1.31 MB with no leaks;
- Starter workflow policy passed with 12 workflows, 13 pinned actions, and five
  explicitly privileged jobs; Template passed with five workflows, nine pinned
  actions, and one pinned scanner image;
- the clean-commit release rehearsal generated 36 digested subjects: seven npm
  packages, 27 Maven files, and populated npm/JVM CycloneDX SBOMs; and
- hosted attestation run
  [`33083933339`](https://github.com/vireocodedev/starter/actions/runs/33083933339)
  verified all 34 exact public subjects, published npm attestation
  [`43426192`](https://github.com/vireocodedev/starter/attestations/43426192) and
  Maven attestation
  [`43426201`](https://github.com/vireocodedev/starter/attestations/43426201), and
  retained evidence artifact
  [`9651461596`](https://github.com/vireocodedev/starter/actions/runs/33083933339/artifacts/9651461596);
  and
- Starter's complete gate passed with its TypeScript/docs portion 11/11 in
  1m 28.638s, followed by JVM/Javadocs and the isolated Maven consumer. Template
  passed 5/5 in 123.568s, within its 420s budget.

## P1-10, P1-11, and P1-13 public-promise tranche

The Starter and Template public-contract policies now bind each advertised setup,
verification, Storybook, and deployment command to the exact hosted workflow that
executes it. Both main verification workflows include a weekly clean-run schedule in
addition to change-triggered execution. The public temporal-values guide publishes
the six canonical field shapes and is read by the UI field test that exercises those
same values.

The framework README now routes evaluators by goal and links dedicated evaluation,
public-API, temporal, and UI-surface guides. The Template README and getting-started
guide define first success as an authenticated Item create/edit/filter/delete loop,
state the development-only credential boundary, and distinguish the current `0.x`
Template from a hosted platform, generator, production claim, or automatic offline
conflict solution.

Starter UI's 13 exported entry points now carry machine-enforced stability,
consumer-guidance, symbol-budget, and growth-disposition fields. This classifies all
1,364 unique exported names without conflicts: ordinary application surfaces are
supported, optional peer integrations are advanced, and four legacy Storybook
authoring surfaces are deprecated with migration guidance and a next-major
extraction disposition.

P1-10, P1-11, and P1-13 completed this tranche. P1-15 remains the final independent
public-alpha gate.

Verification evidence for this tranche:

- Starter public-contract policy passed with 16 required surfaces, seven npm
  workspaces, 120 Markdown files, and three documented commands bound to hosted
  execution evidence.
- Starter API policy classified 22 npm entry points, six Maven modules, 111 public
  JVM declarations, and all 1,364 uniquely named UI exports without stability
  conflicts.
- Starter's authoritative TypeScript/docs gate passed 11/11 in 2m 42.648s, followed
  by the JVM/Javadocs build, six-module publication audit, and isolated versionless
  Maven consumer.
- Template public-contract policy passed with 15 required surfaces, six Starter
  dependencies, 30 Markdown files, and three documented commands bound to hosted
  execution evidence.
- Template's authoritative gate passed 5/5 in 127.764s, below its 420s ceiling. The
  separate production-like deployment smoke then passed the static PWA, API proxy,
  backend readiness, and PostgreSQL health checks.

## P1-12 and P1-14 enforced evidence tranche

Starter and Template now carry byte-identical version `2026-08-27.1` platform
policy snapshots. The machine contract defines 19 rows: nine supported rows require
an executable merge or scheduled lane, while ten compatible, experimental, or
untested rows cannot imply evidence that has not been collected. The local policy
check rejects missing jobs, schedules, documentation rows, and cross-repository
toolchain drift. Template's scheduled sync additionally compares its snapshot with
Starter's public canonical file.

The recurring hosted suites are current. Starter support-evidence run
[`33079434123`](https://github.com/vireocodedev/starter/actions/runs/33079434123)
exercised Java 25, peer floors, PostgreSQL 17/18 upgrade fixtures, a slim clean Node
container, and fresh anonymous npm/Maven consumers. Template support-evidence run
[`33079185460`](https://github.com/vireocodedev/starter-template/actions/runs/33079185460)
passed canonical policy sync, Java 25, Playwright Firefox 153/WebKit 26.5, and
PostgreSQL 17/18 full-stack CRUD.

Both authoritative verification gates now write schema-2 evidence with the canonical
host, cache state, stage/total duration, stage/total peak RSS, warning and failure
thresholds, source identity, and result. Evidence is retained for 90 days, calibration
uses the median of the latest five comparable successful hosted runs, and exceptions
require a cause, owner, expiry, rollback, and approval.

Initial hosted evidence is deliberately preserved without tuning from isolated
samples:

- Starter CI run
  [`33079424215`](https://github.com/vireocodedev/starter/actions/runs/33079424215)
  recorded 238.196s and 3,908,900 KiB peak RSS with no warnings. The immediately
  preceding comparable run passed failure thresholds while warning on its 189.846s
  cold test stage and 316.902s total, proving that warnings are preserved without
  destabilizing the gate.
- Template Verify run
  [`33079165098`](https://github.com/vireocodedev/starter-template/actions/runs/33079165098)
  recorded 164.522s and 2,762,808 KiB peak RSS with no warnings, then passed the
  production-like deployment smoke.

P1-12 and P1-14 are complete. The remaining planning scope is 7–15 active
engineer-days plus external wait: P1-01, P1-03, P1-09, and P1-15.

## Evidence commands

- Starter `corepack npm run public:check`: passed; 12 required surfaces, seven npm
  workspaces, and 115 Markdown files checked.
- Starter `corepack npm run verify:all -- silent`: passed; TypeScript/docs 11/11 in
  1m 28.494s, followed by JVM/Javadocs and the isolated publication consumer. The
  packed npm consumer required registry access because its cache is intentionally
  fresh.
- Template `node scripts/public-contract-policy.mjs`: passed; 12 required surfaces,
  six Starter dependencies, and 30 Markdown files checked.
- Template `./scripts/verify.sh silent`: passed 5/5 in 118.829s and remained within
  the 420s budget. The successful run allowed the Storybook/browser lanes to bind
  localhost; the restricted-sandbox attempt reached Storybook and failed only with
  `listen EPERM`.

The dated commit identifiers are recorded in repository history for this slice.
