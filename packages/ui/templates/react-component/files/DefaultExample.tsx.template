import { VireoStorybookProvider } from "@vireocodedev/starter-ui/storybook";
// TODO(component-author): After publishing the component boundary, import {{componentName}} from "@vireocodedev/starter-ui".
import { {{componentName}} } from "{{componentSourceModule}}";

export default function DefaultExample() {
  return (
    <VireoStorybookProvider>
      <{{componentName}} />
    </VireoStorybookProvider>
  );
}
