import { VireoStorybookProvider } from "@vireocodedev/ui/storybook";
// TODO(component-author): After publishing the component boundary, import {{componentName}} from "@vireocodedev/ui".
import { {{componentName}} } from "{{componentSourceModule}}";

export default function DefaultExample() {
  return (
    <VireoStorybookProvider>
      <{{componentName}} />
    </VireoStorybookProvider>
  );
}
